# 🧪 Тестирование бота

## Автоматические тесты

### Запуск всех тестов

```bash
./run_tests.sh
```

### Что тестируется

1. **Парсинг дат** ✅
   - сегодня, завтра, вчера, позавчера
   - Форматы времени (10:00, 10-00, 10.00)

2. **События (через MCP)** 📆
   - Создание события
   - Просмотр событий
   - Удаление события

3. **Задачи (через API)** ✅
   - Создание задачи
   - Просмотр задач
   - Отметка выполненной
   - Удаление задачи

## Ручное тестирование в Telegram

### Тест 1: Создание события
```
Создай событие "английский" на завтра на 11-00
```
**Ожидается:** Событие создано на завтра в 11:00

### Тест 2: Создание задачи
```
/create_task поменять доллары | завтра
```
**Ожидается:** Задача создана на завтра (без времени)

### Тест 3: Просмотр событий
```
покажи события на позавчера
```
**Ожидается:** Заголовок "Позавчера (16.01.2026)" + список событий

### Тест 4: Просмотр задач
```
/tasks
```
**Ожидается:** Список всех задач

### Тест 5: Комбинированный просмотр
```
/today_all
```
**Ожидается:** События + задачи на сегодня

## Проверка логов

### Логи бота
```bash
tail -f /tmp/bot_fixed.log
```

### Логи тестов
```bash
cat /tmp/test_results.log
```

## Известные ограничения

1. **Google Tasks API не поддерживает время** - только дату
   - Решение: Используйте события календаря для встреч со временем

2. **MCP сервер может не находить только что созданные события**
   - Решение: Подождите несколько секунд и повторите запрос

3. **Задачи не поддерживают повторение**
   - Решение: Используйте повторяющиеся события календаря

## Отладка

### Включить подробное логирование
Отредактируйте `main.py`:
```python
logging.basicConfig(level=logging.DEBUG)
```

### Проверить подключение к MCP
```bash
npx @cocal/google-calendar-mcp auth
```

### Проверить токены Tasks API
```bash
python reauth_tasks_only.py
```
