# 🚀 Быстрый старт

Пошаговая инструкция для запуска бота за 10 минут.

## Шаг 1: Клонирование и установка (2 мин)

```bash
cd /path/to/project
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
npm install -g @cocal/google-calendar-mcp
```

## Шаг 2: Google Cloud Console (3 мин)

1. Откройте [Google Cloud Console](https://console.cloud.google.com)
2. Создайте новый проект
3. Перейдите в **APIs & Services** → **Enable APIs**
4. Включите:
   - Google Calendar API
   - Google Tasks API
5. Перейдите в **Credentials** → **Create Credentials** → **OAuth 2.0 Client ID**
6. Выберите **Desktop app**
7. Скачайте JSON файл
8. Переименуйте в `google_credentials.json` и положите в `credentials/`

## Шаг 3: OAuth Consent Screen (1 мин)

1. В Google Cloud Console → **OAuth consent screen**
2. Выберите **External** → **Create**
3. Заполните название приложения
4. Добавьте свой email в **Test users**
5. Сохраните

## Шаг 4: Telegram Bot (1 мин)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен
5. Создайте файл `.env`:

```bash
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

## Шаг 5: Авторизация (2 мин)

```bash
# Calendar (MCP)
npx @cocal/google-calendar-mcp auth

# Tasks (API)
python reauth_tasks_only.py
```

Откроется браузер → разрешите доступ → готово!

## Шаг 6: Запуск (1 мин)

```bash
./run.sh
```

Или через Docker:

```bash
docker compose up -d
```

## ✅ Проверка

Откройте бота в Telegram и отправьте:

```
/start
/help
```

Попробуйте создать событие:

```
Создай встречу на завтра в 10:00
```

## 🎉 Готово!

Бот работает! Теперь можете:

- Создавать события естественным языком
- Управлять задачами через команды
- Просматривать календарь и задачи

## 🆘 Проблемы?

### Бот не отвечает
```bash
# Проверьте логи
tail -f /tmp/bot_*.log

# Перезапустите
pkill -f "python main.py"
./run.sh
```

### Ошибка авторизации Google
```bash
# Переавторизуйтесь
npx @cocal/google-calendar-mcp auth
python reauth_tasks_only.py
```

### Бот не находит события
Подождите несколько секунд - MCP серверу нужно время для синхронизации.

## 📖 Дальше

- [EXAMPLES.md](EXAMPLES.md) - примеры команд
- [FAQ.md](FAQ.md) - частые вопросы
- [TESTING.md](TESTING.md) - тестирование
